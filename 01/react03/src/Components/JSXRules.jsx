const JSXRules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>JSX must return a single parent element.</p>
        <p>JSX element must return a single parent element.</p>
        <p>JSX attributes are written using camelCase (e.g., className instead of class).</p>
    </div>
  )
}

export default JSXRules